input {
  tcp {
    port => 5044
    codec => "json_lines"
  }
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
	user => "elastic"
    password => "8s*GtRRLmX_XP0htMS7s"
    ilm_enabled => false
	ssl_certificate_verification => false
  }
  logstash {
	hosts => ["http://localhost:5044"]
  }
  stdout {
      codec = rubydebug
   }
}